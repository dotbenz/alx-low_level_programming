#include "main.h"

/**
 * print_alphabet_x10(void) - prints the alphabet to lower case 10times
 *
 * Return: void
 */

void print_alphabet_x10(void)
{
	int c, i;

	for (i = 0, i <= 9; i++)
	{
		for (c = 'a'; j <= 'z'; c++)
		{
			_putchar(c);
		}
		_putchar('\n');

	}
}
